interface:
  display_name: "Take PR to Completion"
  short_description: "Prepare, watch, and explicitly land pull requests"
  default_prompt: "Use $pr-completion:take-pr-to-completion to take the in-scope PRs through checks, incremental reviews, repairs, and protected merge without repeated approval prompts."
