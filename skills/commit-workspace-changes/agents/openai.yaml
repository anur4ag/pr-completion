interface:
  display_name: "Commit Workspace Changes"
  short_description: "Validate and commit task-related local changes"
  default_prompt: "Use $pr-completion:commit-workspace-changes to validate and commit the task-related local changes, then return the commit and validation evidence."
